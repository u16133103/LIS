import { EmployeeType.Model } from './employee-type.model';

describe('EmployeeType.Model', () => {
  it('should create an instance', () => {
    expect(new EmployeeType.Model()).toBeTruthy();
  });
});
